{
  "name": "aicd-map",
  "version": "1.0.0",
  "description": "MapLibre map project",
  "scripts": {
    "dev": "live-server --port=3000 --open=/ --watch=.",
    "start": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
